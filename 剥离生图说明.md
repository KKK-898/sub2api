# 剥离生图说明

## 功能名称

- 配置字段：`boli_shengtu`
- 后台显示：`剥离生图请求`
- 生效范围：仅 OpenAI API Key 类型账号

## 解决的问题

当用户所在分组允许生图时，客户端可能会在 `/v1/responses` 或 `/v1/chat/completions` 文本请求中携带 `image_generation` 工具。

如果命中的上游账号不支持生图，请求原样转发给上游后，上游可能拒绝该工具，导致中转返回 502。

开启账号级 `boli_shengtu` 后，只有命中这个具体上游账号时，网关才会在转发前移除：

- `tools` 数组中的 `type: "image_generation"` 工具
- 指向 `image_generation` 的 `tool_choice`

普通文本、普通函数工具、模型映射、计费、并发和分组权限逻辑保持不变。

## 权限分层

1. 分组层面决定用户是否允许生图。
2. 分组禁止生图时，生图请求会在中转层直接拦截。
3. 分组允许生图时，请求继续进入账号调度。
4. 精准命中某个 OpenAI API Key 上游账号后，如果该账号开启 `boli_shengtu`，只剥离该请求中的生图工具，再转发上游。
5. 同一分组里的其他账号不受影响，仍可正常生图。

## 后台使用方式

添加或编辑账号时：

1. 平台选择 `OpenAI`。
2. 账号类型选择 `API Key`。
3. 开启 `剥离生图请求`。
4. 保存账号。

保存后账号 `extra` 中会出现：

```json
{
  "boli_shengtu": true
}
```

关闭开关后，该字段会被删除。

## 维护方式

如果以后更新上游源码时，这个功能被覆盖，可以通过维护分支或备份补丁重新恢复。

建议长期保留一个自己的功能分支，例如：

```bash
git switch codex/boli-shengtu
git fetch origin
git rebase origin/main
```

如果线上使用 Docker 部署，需要重新构建并部署包含本功能的自定义镜像。不要直接切回官方 `weishaw/sub2api:latest`，否则官方镜像中没有该功能时，`boli_shengtu` 不会生效。

## 相关代码位置

- 后端账号开关：`backend/internal/service/account.go`
- 后端剥离工具：`backend/internal/service/openai_codex_transform.go`
- OpenAI HTTP 转发：`backend/internal/service/openai_gateway_service.go`
- Chat Completions 转发：`backend/internal/service/openai_gateway_chat_completions.go`
- Raw Chat Completions 转发：`backend/internal/service/openai_gateway_chat_completions_raw.go`
- Responses fallback：`backend/internal/service/openai_gateway_responses_chat_fallback.go`
- Responses WebSocket：`backend/internal/service/openai_ws_forwarder.go`
- 添加账号 UI：`frontend/src/components/account/CreateAccountModal.vue`
- 编辑账号 UI：`frontend/src/components/account/EditAccountModal.vue`
- 中英文文案：`frontend/src/i18n/locales/zh.ts`、`frontend/src/i18n/locales/en.ts`
